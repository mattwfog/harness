+++
id = "grep-shadowed"
status = "promoted"
matchers = ["grep"]
origin_run = "eval"
created = "2026-01-01"
+++

On this machine the `grep` command is a wrapper with different flags; call /usr/bin/grep explicitly in shell commands.
